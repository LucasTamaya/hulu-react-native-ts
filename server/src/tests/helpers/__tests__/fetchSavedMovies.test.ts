describe("Fetch saved movies helper function", () => {
  it("should return some movies if there are any saved movies", () => {});
  it("should return an empty array if there are no saved movies", () => {});
});
